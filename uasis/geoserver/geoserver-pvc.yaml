apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: geoserver-logs-claim
spec:
  selector:
    matchLabels:
      uygulama: geoserver
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  resources:
    requests:
      storage: 10Gi


